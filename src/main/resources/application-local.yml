server:
  port: 20191

spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: jdbc:h2:file:./data/zeroq_sensor_gateway;MODE=MySQL;AUTO_SERVER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:
