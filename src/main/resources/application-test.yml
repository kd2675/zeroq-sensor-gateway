server:
  port: 30191

spring:
  config:
    activate:
      on-profile: test
  datasource:
    url: jdbc:h2:mem:gateway_test;MODE=MySQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
  task:
    scheduling:
      enabled: false
