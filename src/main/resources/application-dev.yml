server:
  port: 20191

spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: jdbc:h2:file:./data/zeroq_sensor_gateway_dev;MODE=MySQL;AUTO_SERVER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:
